# ──────────────────────────────────────────────────────────────
#  Farm Bot — Python Dependencies
#  Install:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# ── Core ───────────────────────────────────────────────────
psutil>=5.9.0,<7.0.0            # Process management (kill/launch GnBots, LDPlayer)
pyautogui>=0.9.54               # Mouse/keyboard automation (clicking, screenshots)
Pillow>=10.0.0                  # Image handling (pyautogui dependency, used for basic screen capture)

# ── Windows API ────────────────────────────────────────────
pywin32>=306                    # win32gui, win32con, win32ui, win32api (window management, PostMessage clicks)

# ── Windows-only (optional) ────────────────────────────────
win10toast>=0.9 ; sys_platform == 'win32'       # Desktop toast notifications (Windows 10+)

# ── Diagnostics / Virtual Display ──────────────────────────
WMI>=1.5.1 ; sys_platform == 'win32'            # WMI queries for monitor PNP device IDs (virtual_display.py)
